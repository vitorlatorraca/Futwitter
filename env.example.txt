# Copie este arquivo para .env e preencha com suas credenciais
# IMPORTANTE: Este arquivo deve estar na raiz do projeto (mesmo nível que package.json)

# ============================================
# REQUIRED - Database
# ============================================
# URL de conexão com o banco de dados PostgreSQL
# Exemplo para Neon: postgresql://usuario:senha@ep-xxx-xxx.region.aws.neon.tech/database?sslmode=require
# Exemplo para PostgreSQL local: postgresql://postgres:senha@localhost:5432/brasileirao
DATABASE_URL=postgresql://usuario:senha@host:porta/database

# ============================================
# REQUIRED - Server
# ============================================
# Porta do servidor (padrão: 5000)
# Railway/Vercel fornecem PORT automaticamente - não defina em produção
PORT=5000

# ============================================
# REQUIRED - Authentication Secrets
# ============================================
# Chave secreta para sessões (gere uma string aleatória segura)
# Você pode gerar uma com: openssl rand -base64 32
# IMPORTANTE: Use valores diferentes em produção!
SESSION_SECRET=sua-chave-secreta-aqui-mude-em-producao

# Chave secreta para JWT tokens (OBRIGATÓRIO)
# Gere uma com: openssl rand -base64 32
# IMPORTANTE: Use valores diferentes em produção!
JWT_SECRET=sua-jwt-secret-aqui-mude-em-producao

# ============================================
# PRODUCTION - Required for Railway + Vercel
# ============================================
# URL do frontend em produção (para CORS e cookies)
# Exemplo: https://seu-app.vercel.app
# Railway precisa desta variável para permitir requisições do frontend
FRONTEND_URL=

# URL do backend em produção (opcional, Railway fornece automaticamente)
# Railway usa: RAILWAY_PUBLIC_DOMAIN ou você pode definir BACKEND_URL
# Exemplo: https://seu-backend.railway.app
BACKEND_URL=

# ============================================
# OPTIONAL - Frontend API URL
# ============================================
# URL base para o frontend acessar a API
# IMPORTANTE: Em desenvolvimento local, DEIXE EM BRANCO
# Em produção (Vercel), defina como a URL do backend (Railway)
# Exemplo: https://seu-backend.railway.app
VITE_API_BASE_URL=

# ============================================
# OPTIONAL - Cookie Domain (não necessário para Railway + Vercel)
# ============================================
# COOKIE_DOMAIN=.seudominio.com

